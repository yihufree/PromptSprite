# 20260829_软件安装使用说明书_v6.md

> **本说明书对应软件版本：V1.4.0（2026-08-29 发布）**。数据无价，请养成定期备份的习惯。

---

## 目录
1. [简介](#简介)
2. [系统要求](#系统要求)
3. [安装指南](#安装指南)
4. [首次启动与初始化](#首次启动与初始化)
5. [界面概览](#界面概览)
6. [核心功能操作](#核心功能操作)
   - 6.1 项目类别与根目录
   - 6.2 创建分类与条目
   - 6.3 编辑与删除
   - 6.4 复制与移动
   - 6.5 搜索与筛选
   - 6.6 一键复制
   - 6.7 导入与导出
   - 6.8 备份与恢复（全量 + 增量）
   - 6.9 老版本数据迁移
   - 6.10 设置
7. [常见问题与排查](#常见问题与排查)
8. [卸载与清理](#卸载与清理)
9. [技术支持与反馈](#技术支持与反馈)
10. [许可证与免责声明](#许可证与免责声明)

---

## 简介
PromptSprite（提示精灵）是一款本地运行的 AI 提示词管理工具。它提供**五级分类书架**（项目类别/根目录/一级分类/二级分类/条目）、**极速全文检索**、**一键复制**、**多格式导入导出**、**自动全量备份**与**每日增量备份**（支持换机合并）。所有数据（提示词、分类、配图、备份）均存储在您电脑的 `data/` 目录中，**绝不联网**、**无任何隐私上传**。

内置内容：14 个根目录、约 **3934 条**提示词，按 5 个项目类别归类，开箱即用。

---

## 系统要求
| 项目 | 最低要求 | 推荐 |
|------|----------|------|
| 操作系统 | Windows 10 (64-bit) / Windows 11 | Windows 11 22H2+ |
| 内存 | 4 GB | 8 GB+ |
| 磁盘 | 500 MB 可用空间 | 2 GB+（便于备份与导入） |
| 显示 | 1366 × 768 | 1920 × 1080 或更高 |
| 运行时 | 无需额外安装，EXE 独立运行 | — |

---

## 安装指南
### 方式一：直接使用预编译 EXE（推荐）
1. 下载 `PromptSprite_v1.4.0.zip`（或 `.exe`）。
2. 解压到任意**英文路径**，如 `D:\PromptSprite\`。
3. 双击 `PromptSprite.exe` 运行。

### 方式二：从源码运行（开发者）
```powershell
pip install -r requirements.txt
python -m app.main
```

---

## 首次启动与初始化
- **自动建库**：首次运行自动创建 5 个项目类别、预置根目录并导入内置提示词（约 3934 条），耗时约 5‑30 秒，请勿关闭窗口。
- **老数据迁移**：若检测到旧版本数据（根目录未归入项目类别），会自动弹出**数据迁移向导**：核对每个根目录归入的项目类别（默认已按映射预填），点击"执行迁移"即可；未匹配的默认归入"未明确分类"。取消可稍后在 **导入菜单 → 数据迁移向导…** 再次打开。
- **电脑代号**：增量备份用，首次退出时自动取本机主机名，可在设置中修改。

---

## 界面概览
```
┌──────────────────────────────────────────────────────────────────────┐
│  标题栏：PromptSprite v1.4.0       🔒锁定 ⚙设置 ⇩导入 ⇧导出 ✚快速新建  │
├────────┬────────┬────────┬────────┬──────────────────────────────────┤
│ 项目类别│  根目录  │ 一级分类 │ 二级分类 │       条目列表 / 详情区        │
│ 日常学习│ 视频    │ 分类A   │ 子分类X │  - 卡片/列表视图切换            │
│ 网上资源│ 图像    │ 分类B   │ 子分类Y │  - 单击选中，双击编辑            │
│ 个人梳理│ …      │ …      │ …      │  - 右键菜单                      │
│ …      │        │        │        │  - 悬停预览（防抖 300ms）          │
├────────┴────────┴────────┴────────┴──────────────────────────────────┤
│  底部状态栏：总提示词数 ｜ 项目类别 ｜ 根目录 ｜ 一级 ｜ 二级 统计        │
└──────────────────────────────────────────────────────────────────────┘
```
- 各列宽度：项目类别/根目录 ≤8 汉字，一级/二级/条目 ≤12 汉字（长名称自动截断，**悬停鼠标即弹出完整名称提示**）。
- **🗂 项目列**（工具栏按钮）：可折叠/展开"项目类别"列，收起后留出更多空间给分类与条目区。
- 单击/悬停"项目类别" → 显示其下根目录；逐级进入根目录/一级/二级分类。
- **🗂 未分配**：存在未归入项目类别的根目录时，项目类别列末尾出现"🗂 未分配"，用于查看与整理。
- **🔒 锁定**：锁定后禁止删除/保存/快速新建/导入等写操作，仅可查询与复制。

---

## 核心功能操作

### 6.1 项目类别与根目录
- **新增项目类别**：项目类别列顶部"➕ 新增项目类别"，或右键项目类别 → 新增。
- **重命名/删除项目类别**：右键 → 重命名/删除；删除前会提示其下根目录去向（默认归入"未明确分类"）。
- **新增根目录**：根目录列顶部"➕ 新增根目录"，输入名称后**选择所属项目类别**（未选择则归入"未明确分类"）。
- **移动根目录到其他项目类别**：右键根目录 → "移动到其他项目类别…"选择目标。

### 6.2 创建分类与条目
1. 选中项目类别 → 选中根目录 → "➕ 新增一级分类 / 新增二级分类"。
2. 条目区"✚ 快速新建"：左侧四列导航（项目类别→根目录→一级→二级），**悬停**项目/根目录/分类即自动联动并锁定目标，光标自动进入名称框，连续录入。

### 6.3 编辑与删除
- **编辑**：选中条目 → 右侧详情区修改 → "💾 保存"。
- **删除**：右键 → 删除 → 二次确认；删除分类其下条目自动转入"未分类"（不丢失）。

### 6.4 复制与移动
- 右键根目录/一级/二级分类 → "复制到…/移动到…"；目标树按 项目类别→根目录→分类 组织；
- 跨项目移动根目录＝调整归属；复制＝生成副本（重名自动加序号），源保留。

### 6.5 搜索与筛选
- 顶部搜索框实时全文匹配；清空恢复当前视图；"📂 未分类"、"⭐ 常用"快速入口。

### 6.6 一键复制
- 选中条目 → "📋 复制全部 / 复制中文 / 复制英文"；⑩图像方案可点"打开"直达链接。
- **提示**：剪贴板内容对当前用户所有程序可见，请勿复制敏感信息。

### 6.7 导入与导出
| 格式 | 导入 | 导出 | 说明 |
|------|------|------|------|
| JSON | ✅ | ✅ | v2/v3 兼容；v3 含项目类别归属（增量备份用 v3） |
| Excel (.xlsx) | ✅ | ✅ | 表头含 领域/一级/二级/名称 等；**限制 ≤20MB**（流式读取） |
| Markdown | ✅ | ❌ | 内置手册导入 |
| HTML | ❌ | ✅ | 已转义，可浏览器打开分享 |

> 提示：Excel 导入的新根目录（表头"领域"中不存在的名称）将自动归入"未明确分类"，导入后可自行调整。

### 6.8 备份与恢复（全量 + 增量）
**全量备份（自动）**：每次启动自动保留最近 5 份 `data/backup/prompts_时间戳.db`；恢复＝关闭软件后用备份覆盖 `data/prompts.db`。

**增量备份（每日）**：每次关闭软件自动把"当日新增/修改"备份为
`data/backup/incremental/增量_{电脑代号}_{日期}.json`（当日再次新增后关闭会自动更新该文件）。
- **覆盖范围**：当日新增/修改的条目与分类（含"仅新建、还没有条目的空分类"）以及**当日删除**（删除清单），换机导入时能同步增、删、改与分类结构。
- 换机合并：导入菜单 → "导入增量备份…" → 选增量 JSON → 自动判重合并，并同步删除清单；
- 浏览导出：导出菜单 → "增量数据导出 Excel…/HTML…"；
- 文件携带：导出菜单 → "导出增量备份文件到…"（复制到 U 盘等）。

### 6.9 老版本数据迁移
- 旧版（根目录-一级-二级）数据升级后，根目录暂处"未分配"；
- 通过"数据迁移向导…"逐项核对映射并执行；未选择/未匹配自动归入"未明确分类"，**数据不会丢失**。

### 6.10 设置
1. 记住窗口大小；2. 默认视图（卡片/列表）；3. 详情字段显示（自动/全部/精简）；
4. **电脑代号**（增量备份区分多机）；5. **增量保留天数**（默认 30）；6. 关于。

---

## 常见问题与排查
| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 启动闪退 / 无反应 | 杀毒拦截 / 路径含中文 | 加入白名单、改用纯英文路径 |
| 升级后根目录都在"未分配" | 尚未执行迁移 | 打开"数据迁移向导…"执行迁移 |
| 找不到增量备份文件 | 当日无新增不生成 | 新增/修改后正常退出即可 |
| 导入 Excel 卡死 | 文件过大（>20 MB） | 拆分文件或用 JSON 导入 |
| 全局热键无效 | 被占用 / 权限不足 | 以管理员身份运行一次 |
| 列中名称显示不全 | 列宽较窄（设计如此） | 悬停即可选中查看 |

---

## 卸载与清理
- 便携版：直接删除整个解压文件夹（含 `data/`）。
- 保留数据：先复制 `data/prompts.db` 与 `data/backup/` 到别处。
- 无残留：不写注册表、不装系统服务。

---

## 技术支持与反馈
- 问题/建议请在项目 GitHub 仓库 Issues 提交（若尚未发布，请联系开发组）。
- 安全漏洞请通过私有渠道报告。

---

## 许可证与免责声明
- 本软件基于 **MIT License** 开源，仅供学习与个人使用。
- 作者不对因使用本软件导致的任何数据丢失、隐私泄露或业务损失承担责任。
- 请务必定期把 `data/prompts.db` 与增量备份文件备份到异地存储（网盘、U 盘、NAS 等）。

---

*文档版本：2026-08-29 V6（对应软件 V1.4.0）*
